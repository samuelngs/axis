
etcd:
  endpoints:
    - http://127.0.0.1:2379
daemon:
  discovery: /announce/service/rethinkdb-haproxy
  entrypoint: rethinkdb
  master:
    - "--bind"
    - "all"
  slave:
    - "--bind"
    - "all"
    - "{{with index . 0}}--join {{.IP}}:{{.Port}}{{end}}"
